overwrite: true
schema: graph/schema/*
documents: operations/*
generates:
  src/schemas.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
      - typescript-apollo-client-helpers
  ./graphql.schema.json:
    plugins:
      - "introspection"
