query getMe {
  me {
    email
    createdProjects {
      name
      description
    }
  }
}
mutation createProject($newProject: NewProjectInput!) {
  createProject(project: $newProject) {
    id
  }
}
